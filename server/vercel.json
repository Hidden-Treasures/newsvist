{
  "version": 2,
  "builds": [
    {
      "src": "./src/server.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["package.json"]
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "./src/server.ts"
    },
    {
      "src": "/(.*)",
      "dest": "./src/server.ts"
    }
  ]
}
